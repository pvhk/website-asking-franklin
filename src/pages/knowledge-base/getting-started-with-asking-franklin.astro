---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { KnowledgeArticle } from '@/pages/_KnowledgeArticle';
import { getContent } from '@/lib/i18n';

const lang = 'en';
const content = getContent(lang);
const article = content.knowledgeBase.articles[0];
---

<BaseLayout
  lang={lang}
  metaTitle={`${article.title} - ${content.siteName}`}
  metaDescription={article.excerpt}
  currentPath="/knowledge-base/getting-started-with-asking-franklin"
  breadcrumbs={[
    { name: 'Home', url: 'https://www.askingfranklin.com/' },
    { name: 'Knowledge Base', url: 'https://www.askingfranklin.com/knowledge-base' },
    { name: article.title },
  ]}
  articleData={{
    headline: article.title,
    description: article.excerpt,
    datePublished: '2025-12-01',
    dateModified: '2025-12-01',
  }}
>
  <KnowledgeArticle lang={lang} client:load />
</BaseLayout>
